name: Manual Server Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Add SSH Key to Agent
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Copy Configs via SCP
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "mkdir -p /var/www/polkanoval-project/nginx/conf/ /var/www/polkanoval-project/media/"
          scp -o StrictHostKeyChecking=no docker-compose.yml ${{ secrets.USER }}@${{ secrets.HOST }}:/var/www/polkanoval-project/
          scp -o StrictHostKeyChecking=no nginx/conf/prod.conf ${{ secrets.USER }}@${{ secrets.HOST }}:/var/www/polkanoval-project/nginx/conf/

      - name: Run deploy scripts on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/polkanoval-project

            # 1. Авторизация
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # 2. Обновление образов
            docker compose pull

            # 3. ЗАПУСК ЯДРА (Без блокировки терминала)
            # Запускаем только redis и backend. Остальные (celery, nginx) будут ждать их готовности.
            docker compose up -d redis backend

            # 4. ПОДГОТОВКА ПРАВ И БД (Важно сделать ДО миграций)
            sudo chmod -R 775 media/
            [ -f db.sqlite3 ] || touch db.sqlite3
            sudo chmod 666 db.sqlite3

            # Даем Django пару секунд на старт процесса
            sleep 5

            # 5. АВТОМАТИЗАЦИЯ DJANGO (Разобьет Deadlock)
            # Эти команды оживят backend, чтобы он прошел healthcheck
            docker compose exec -T backend python manage.py migrate --noinput
            docker compose exec -T backend python manage.py collectstatic --noinput

            # 6. ЗАПУСК ОСТАЛЬНЫХ СЕРВИСОВ
            # Теперь, когда миграции прошли, backend станет Healthy, и Docker запустит воркеры и nginx
            docker compose up -d --remove-orphans

            # 7. ОЧИСТКА
            docker system prune -af --volumes

      - name: Verify Deployment Health
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/polkanoval-project
            echo "⏳ Ожидание финальной готовности (40 сек)..."
            sleep 40

            echo "📊 ПРОВЕРКА API:"
            # Используем python для проверки бэкенда
            HEALTH_JSON=$(docker compose exec -T backend python -c "import urllib.request; print(urllib.request.urlopen('http://localhost:8000/api/health/').read().decode())" || echo "error")
            echo "Response: $HEALTH_JSON"

            echo "📊 ПРОВЕРКА ФРОНТЕНДА:"
            # Используем wget для фронтенда (в nginx:alpine нет python)
            if docker compose exec -T frontend wget --spider -q http://localhost; then
              echo "{\"frontend\": \"ok\"}"
            else
              echo "{\"frontend\": \"error\"}"
            fi

            if [[ "$HEALTH_JSON" == *"ok"* ]]; then
              echo "✅ ДЕПЛОЙ УСПЕШЕН: Все системы работают исправно."
            else
              echo "❌ ВНИМАНИЕ: Обнаружены проблемы в работе API!"
              echo "ПОСЛЕДНИЕ ЛОГИ БЭКЕНДА:"
              docker compose logs backend --tail 50
              exit 1
            fi