name: Manual Server Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Add SSH Key to Agent
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Copy Configs via SCP
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "mkdir -p /var/www/polkanoval-project/nginx/conf/ /var/www/polkanoval-project/media/"
          scp -o StrictHostKeyChecking=no docker-compose.yml ${{ secrets.USER }}@${{ secrets.HOST }}:/var/www/polkanoval-project/
          scp -o StrictHostKeyChecking=no nginx/conf/prod.conf ${{ secrets.USER }}@${{ secrets.HOST }}:/var/www/polkanoval-project/nginx/conf/

      - name: Run deploy scripts on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/polkanoval-project

            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose pull

            # 1. ЖЕСТКИЙ FIX ПРАВ (SSL + SQLite)
            # Открываем доступ к сертификатам для Nginx (симлинки Certbot)
            # В 2026 без 755 на папку archive симлинки в live не работают
            sudo chmod -R 755 /etc/letsencrypt/
            sudo find /etc/letsencrypt/archive/ -type f -exec chmod 644 {} +

            # Права на базу (SQLite)
            [ -f db.sqlite3 ] || touch db.sqlite3
            sudo chmod 666 db.sqlite3
            sudo chmod 777 .
            mkdir -p media && sudo chmod -R 777 media

            # 2. ПОЛНЫЙ ПЕРЕЗАПУСК
            docker compose down
            docker compose up -d --remove-orphans

            # 3. МИГРАЦИИ (Лечим структуру 'order' и статику)
            docker compose exec -T backend python manage.py makemigrations order --noinput
            docker compose exec -T backend python manage.py migrate --noinput
            docker compose exec -T backend python manage.py collectstatic --noinput

            # 4. СИНХРОНИЗАЦИЯ СЕРВИСОВ
            docker compose restart backend nginx
            docker system prune -f

      - name: Verify Deployment Health
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/polkanoval-project
            echo "⏳ Ожидание запуска Nginx и API..."
            sleep 15

            # Проверка, что Nginx вообще поднял порты 80/443
            if [ "$(docker inspect -f '{{.State.Running}}' pizza-nginx)" != "true" ]; then
              echo "❌ NGINX УПАЛ: Ошибка SSL или конфига!"
              docker compose logs nginx | tail -n 20
              exit 1
            fi

            # Проверка API (Database/Redis)
            for i in {1..10}; do
              echo "📊 Проверка API #$i..."
              RESPONSE=$(docker compose exec -T backend python -c "
            import urllib.request, json
            req = urllib.request.Request('http://localhost:8000/api/health/')
            req.add_header('Host', 'localhost')
            try:
                with urllib.request.urlopen(req, timeout=5) as r:
                    print(r.read().decode())
            except Exception as e:
                print('waiting')
            " || echo "error")

              echo "Backend: $RESPONSE"

              # Успех только если и база, и редис ответили OK
              if [[ "$RESPONSE" == *"database\": \"ok\""* ]] && [[ "$RESPONSE" == *"redis\": \"ok\""* ]]; then
                echo "✅ ДЕПЛОЙ УСПЕШЕН: Все системы онлайн."
                exit 0
              fi
              sleep 5
            done

            echo "❌ ТАЙМАУТ: Бэкенд не ответил за 60 сек."
            docker compose logs backend | tail -n 20
            exit 1